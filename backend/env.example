# Example backend environment variables (no secrets)
DATABASE_URL=sqlite:///db.sqlite3
DJANGO_SECRET_KEY=replace-me
DJANGO_DEBUG=False
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=
FORCE_CONSOLE_EMAIL=0

# Create a `backend/.env.local` (gitignored) with real secrets for local development.